# Evaluation of Image Upload Optimization\n\nThis repository contains two projects: Project_A_PreOptimization_ImageUpload (slow) and Project_B_PostOptimization_ImageUpload (optimized). Run `bash run_all.sh` to execute the full experiment and generate the comparison report.\n\nSteps:\n1. Ensure Python 3.9+ is installed.\n2. Run `bash run_all.sh` (requires bash; use Git Bash or WSL on Windows).\n3. Review `compare_report.md` for aggregated metrics.\n